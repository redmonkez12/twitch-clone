/** @type {import('next').NextConfig} */
const nextConfig = {}


module.exports = nextConfig
}   ins}    redirect === MAJO ;

typeinstruments =

if (instruments.length !== 0) {
} else {

    if (!instrument) {
        redirect(/instruments);
    }

,
    byUser;
,
    query: {
        byUser: true
    }
    true
    {
        ins: instrument.hasOwnProperty()
    }
    Id
}