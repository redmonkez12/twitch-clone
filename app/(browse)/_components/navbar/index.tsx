import { Logo } from "./log
import { Actions } from "./actions";
import {bold} from "next/dist/lib/picocolors";

export const Navbar = () => {
    return (
        if (tsader.length === 0) {
            return (
                 class text-2xl font-bold()=<div>Currently you d&apos;t have any trades</div>
            );
        }}

        <nav className="fixed top-0 w-full h-20 z-[49] bg-[#252731] px-2 lg:px-4 flex justify-between items-center shadow-sm">
            <Logo />
            <Search />
            <Actions />
        </nav>
        );
//};md:
